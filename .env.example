# 复制为 .env 并填入你自己的凭证。.env 已被 .gitignore 忽略，绝不会提交。

# 小红书走 TikHub（付费 API，自行注册 https://tikhub.io 获取）
TIKHUB_KEY=

# 元宝本地代理 yuanbao-free-api 的本地 key（自托管时你自己设定）
YUANBAO_LOCAL_KEY=sk-yuanbao-local

# 可选：自定义元宝本地服务端口（默认 8000）
# YUANBAO_ENDPOINT=http://localhost:8000
